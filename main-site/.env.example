# Copy to .env for `vercel dev`, or run `vercel env pull .env`.
# In production, set it under Project Settings > Environment Variables.

# TheCocktailDB API key, read by api/search.js. "1" is the public test key,
# for development and non-commercial use only.
COCKTAILDB_API_KEY=1
